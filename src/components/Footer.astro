---
import '../styles/footer.css';
import MedalSettingsPanel from "./MedalSettingsPanel.astro";
import HeroSelectView from './HeroSelectView.astro';
import HeroDisplayPanel from './HeroDisplayPanel.astro';

import { ImageAsset } from '../types/common';
import { getImageAssetById } from "../accessors/common/getData.ts";

const MIN_HERO_LEVEL = 1;
const MAX_HERO_LEVEL = 9;

const getAssetPath = (asset: ImageAsset) => {
  const imageAsset = getImageAssetById(asset);
  return imageAsset ? `/CompassDeckSimulator/${imageAsset.imagePath}` : '';
};

const SETTING_MEDAL_IMAGE_PATH = getAssetPath(ImageAsset.SETTING_MEDAL);
---
<footer class="footer">
  <div class="footer-content">
    <div class="dropdown-wrapper">
      <div class="dropdown">
        <button id="dropdown-trigger" class="dropdown-trigger" type="button">
          <span class="trigger-content">ヒーローを選択してね</span>
        </button>
      </div>

      <div class="level-button-wrap">
        <button class="level-button" id="level-select-trigger" type="button" aria-label="レベル設定" aria-haspopup="listbox" aria-expanded="false">Lv.{MIN_HERO_LEVEL}</button>
        <div id="level-select-menu" class="level-select-menu" role="listbox" aria-hidden="true">
          {Array.from({ length: MAX_HERO_LEVEL - MIN_HERO_LEVEL + 1 }, (_, index) => {
            const level = index + MIN_HERO_LEVEL;
            return (
              <button
                class="level-select-option"
                type="button"
                role="option"
                data-level={level}
              >
                Lv.{level}
              </button>
            );
          })}
        </div>
      </div>

      <button class="medalSetting" id="medal-settings-trigger" type="button" aria-label="メダル設定">
        <img src={SETTING_MEDAL_IMAGE_PATH} width="32" height="32" alt="" />
      </button>
    </div>
  </div>
</footer>

<HeroSelectView />
<HeroDisplayPanel />

<script>
  const levelButton = document.getElementById('level-select-trigger');
  const levelMenu = document.getElementById('level-select-menu');
  const levelOptions = document.querySelectorAll<HTMLElement>('.level-select-option');
  const MIN_HERO_LEVEL = 1;
  const MAX_HERO_LEVEL = 9;
  const DEFAULT_HERO_LEVEL = MIN_HERO_LEVEL;
  const LEVEL_MENU_WIDTH_PX = 108;
  const LEVEL_MENU_OPTION_HEIGHT_PX = 26;
  const LEVEL_MENU_OPTION_GAP_PX = 4;
  const LEVEL_MENU_PADDING_PX = 6;
  const VIEWPORT_EDGE_PADDING_PX = 16;
  const LEVEL_MENU_OFFSET_PX = 8;

  const closeLevelMenu = () => {
    levelMenu?.classList.remove('is-open');
    levelMenu?.setAttribute('aria-hidden', 'true');
    levelButton?.setAttribute('aria-expanded', 'false');
  };

  const positionLevelMenu = () => {
    if (!levelButton || !levelMenu) return;

    const rect = levelButton.getBoundingClientRect();
    const viewportHeight = window.innerHeight;
    const viewportWidth = window.innerWidth;
    const levelOptionCount = MAX_HERO_LEVEL - MIN_HERO_LEVEL + 1;
    const levelMenuContentHeight = levelOptionCount * LEVEL_MENU_OPTION_HEIGHT_PX;
    const levelMenuGapHeight = (levelOptionCount - 1) * LEVEL_MENU_OPTION_GAP_PX;
    const levelMenuPaddingHeight = LEVEL_MENU_PADDING_PX * 2;
    const menuWidth = Math.min(LEVEL_MENU_WIDTH_PX, viewportWidth - VIEWPORT_EDGE_PADDING_PX * 2);
    const menuHeight = Math.min(
      levelMenuContentHeight + levelMenuGapHeight + levelMenuPaddingHeight,
      viewportHeight - VIEWPORT_EDGE_PADDING_PX * 2
    );
    const spaceBelow = viewportHeight - rect.bottom - VIEWPORT_EDGE_PADDING_PX;
    const spaceAbove = rect.top - VIEWPORT_EDGE_PADDING_PX;
    const openAbove = spaceBelow < menuHeight && spaceAbove > spaceBelow;

    const left = Math.min(
      viewportWidth - menuWidth - VIEWPORT_EDGE_PADDING_PX,
      Math.max(VIEWPORT_EDGE_PADDING_PX, rect.left + rect.width / 2 - menuWidth / 2)
    );
    const top = openAbove
      ? Math.max(VIEWPORT_EDGE_PADDING_PX, rect.top - menuHeight - LEVEL_MENU_OFFSET_PX)
      : Math.min(viewportHeight - menuHeight - VIEWPORT_EDGE_PADDING_PX, rect.bottom + LEVEL_MENU_OFFSET_PX);

    levelMenu.style.left = `${left}px`;
    levelMenu.style.top = `${top}px`;
    levelMenu.style.width = `${menuWidth}px`;
  };

  const openLevelMenu = () => {
    if (!levelButton || !levelMenu) return;
    positionLevelMenu();
    levelMenu.classList.add('is-open');
    levelMenu.setAttribute('aria-hidden', 'false');
    levelButton.setAttribute('aria-expanded', 'true');
  };

  levelButton?.addEventListener('click', (event) => {
    event.stopPropagation();
    if (levelMenu?.classList.contains('is-open')) {
      closeLevelMenu();
    } else {
      openLevelMenu();
    }
  });

  levelOptions.forEach((option) => {
    option.addEventListener('click', () => {
      const level = Number(option.dataset.level ?? DEFAULT_HERO_LEVEL);
      if (levelButton) {
        levelButton.textContent = `Lv.${level}`;
      }
      document.dispatchEvent(new CustomEvent('footer:level-selected', {
        detail: { level },
      }));
      closeLevelMenu();
    });
  });

  window.addEventListener('resize', () => {
    if (levelMenu?.classList.contains('is-open')) {
      positionLevelMenu();
    }
  });

  document.addEventListener('click', (event) => {
    const target = event.target;
    if (!(target instanceof Node)) return;
    if (levelButton?.contains(target) || levelMenu?.contains(target)) {
      return;
    }
    closeLevelMenu();
  });
</script>

<!-- 詳細設定アイコン -->
<script>
  const trigger = document.getElementById("medal-settings-trigger");
  const panel = document.getElementById("medal-settings");

 // トリガークリックで開閉
  trigger?.addEventListener("click", (e) => {
    e.stopPropagation();
    trigger?.classList.toggle('is-active');
    panel?.classList.toggle("is-hidden");
  });

    // パネル外クリックで閉じる
  document.addEventListener("click", (event) => {
    const target = event.target;
    if (target instanceof Node && panel?.contains(target)) {
      return;
    }

    if (!panel?.classList.contains("is-hidden")) {
      trigger?.classList.remove('is-active');
      panel?.classList.add("is-hidden");
    }
  });
</script>